import cardUtilities from "../cardUtilities";

export default class testUtilities {
  constructor() {
    this.orderedDeck = cardUtilities.getOrderedDeckCards();
  }

  loadTableCardsWithMatching() {
    // const tc = this.testTableCards;
    // let idx = 0;
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(0, 0, 0, 0);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(0, 0, 0, 1);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(0, 0, 0, 2);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(1, 1, 1, 0);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(1, 1, 1, 1);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(1, 1, 1, 2);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(2, 2, 2, 0);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(2, 2, 2, 1);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(2, 2, 2, 2);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(0, 1, 2, 0);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(1, 2, 0, 1);
    // tc[idx++] = cardUtilities.getCardInfoFromAttrs(2, 0, 1, 2);
  }
}
