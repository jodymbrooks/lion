// import computerPlayer from '../computerPlayer';
// import cardUtilities, { tableMinCards } from '../cardUtilities';

describe("computer player", () => {
  it("should remember shown cards with varying degrees of accuracy", () => {});
  it("should pick a first card from memory if there is a match in memory", () => {});
  it("should pick an unseen card if memory does not hold a match", () => {});
  it("should pick a second card at random if a match does not exist in memory", () => {});
  it("should pick a second card from memory that matches the selected card if a match in memory exists", () => {});
  it("should pick a third card from memory that matches the selected cards if a match in memory exists", () => {});
  it("should pick a fourth card from memory that matches the selected cards if a match in memory exists", () => {});
  it("should pick a fifth card from memory that matches the selected cards if a match in memory exists", () => {});
  it("should never pick a sixth card even if multiple attrs are matching in the selected cards", () => {});
  it("should decide to pick another card if at least two attrs match and not more than 5 cards have been selected", () => {});
  it("should keep the selected matching cards if there is only a single matching attr unless it's picking from a known seen-cards match", () => {});
  it("should support varying degrees of memory success depending on level using a randomness range", () => {});
  it("should ", () => {});
});
